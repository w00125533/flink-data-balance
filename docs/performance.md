# 性能压测失败记录

| 日期 | Benchmark / Run | Sink / 规模 | 失败现象 | 根因 | 处理建议 | 报告 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-25 | `starrocks-5000-backpressure-opt-20260725-154028` / `starrocks-5000-backpressure-opt-20260725-154028-starrocks-cells5000-chr-eps30` | StarRocks，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Source `360s`，配置并行度 `12` | `deploy submit` 阶段失败，未进入 source 发数；报告显示 `timed out waiting for Flink slots to become available: required=12` | 当前本地 Flink runtime 实际只有 `slots-total=6`、`slots-available=6`，但临时 env 文件 `benchmark-runner/conf/.env.benchmark-local-p12-t6.tmp` 要求 `FDB_FLINK_PARALLELISM=12`；runtime 没有按 p12 重建 | 该轮不计入 StarRocks sink 能力判断；若要验证 p12，需要先用同一 env 重建 Flink JobManager/TaskManager 并确认 `/overview` 可用 slot 不小于 12；本次改用 `.env.local` 的 p6 配置复跑 | `benchmark-runner/output/benchmark-runs/starrocks-5000-backpressure-opt-20260725-154028/index.html` |
| 2026-07-25 | `starrocks-5000-backpressure-opt-p6-20260725-154413` / `starrocks-5000-backpressure-opt-p6-20260725-154413-starrocks-cells5000-chr-eps30` | StarRocks，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Source `360s`，命令行覆盖并行度 `6` | `deploy submit` 阶段仍失败，未进入 source 发数；报告仍显示 `required=12` | benchmark runner 的 `--set FDB_FLINK_PARALLELISM=6` 以进程环境传给 `deploy.sh`，但 `deploy.sh local submit` 会重新 `source FDB_ENV_FILE`，临时 env 文件里的 `FDB_FLINK_PARALLELISM=12` 覆盖了命令行值 | 该轮不计入 StarRocks sink 能力判断；短期使用与 runtime 匹配的 env 文件运行，例如 `.env.local`；后续可考虑修正 `deploy.sh` 的 env 优先级，让 benchmark `--set` 对 Flink runtime 参数也具备最终优先级 | `benchmark-runner/output/benchmark-runs/starrocks-5000-backpressure-opt-p6-20260725-154413/index.html` |
| 2026-07-25 | `starrocks-5000-latestpm-20260725-105641` / `starrocks-5000-latestpm-20260725-105641-starrocks-cells5000-chr-eps30` | StarRocks，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Source `360s`，Flink 并行度 `6`，诊断拆链 `ON` | benchmark 判定 `UNSTABLE`，原因 `sustained backpressure ratio 0.775`；另有 `checkpoint failed=1`，但 Task failed `0`、Sink failures `0`、StarRocks failed load `0`，作业由 runner 正常取消 | JobManager 日志显示作业提交后约 2 秒触发一次 checkpoint，当时 `Source: chr-source -> chr-source-metrics -> to-chr-env (1/6)` 尚未执行，Flink 以 `Not all required tasks are currently running` 放弃该次 checkpoint；之后 `22` 次 checkpoint 均成功 | 该 checkpoint 失败属于启动期触发噪声，不计入 StarRocks Sink 写入能力失败；本轮真实不稳定点仍是高反压和累计延迟，后续可在报告中区分启动期 checkpoint 失败与运行期 checkpoint 失败 | `benchmark-runner/output/benchmark-runs/starrocks-5000-latestpm-20260725-105641/index.html` |
| 2026-07-25 | `starrocks-5rounds-6min-p6-t6-20260725-0549` / `starrocks-5rounds-6min-p6-t6-20260725-0549-starrocks-cells40000-chr-eps30` | StarRocks，`40000` 小区，Target CHR `30/小区/s`，Global CHR `1200000 EPS`，Target PM `1/小区/s`，Source `360s`，Flink 并行度 `6`，CHR producer 线程 `6` | 报告判定 `FAILED`，原因 `Flink job status RESTARTING`；CHR 实际发布约 `5506万`、`152951 EPS`，backlog 约 `3.77亿`；PM 实际约 `36333 EPS`；StarRocks probe 健康，`starrocks rows=719893` | Flink exception history 显示 `java.lang.OutOfMemoryError: Java heap space`，发生在 `user-event-anomaly-detect-dedup -> user-anomaly-metrics (1/6)` 做 Heap keyed state checkpoint snapshot 时；高压下用户级异常去重/状态快照占用堆内存过大，触发作业 `RESTARTING`，不是 StarRocks connector 写入失败 | 本轮保留为本机 `p6/t6` 的 Flink 状态/checkpoint 内存边界样本；后续若继续冲 40k 档，应优先迁到 external-yarn/多 TaskManager，或切 RocksDB state backend、收紧用户级异常去重状态 TTL/窗口状态、降低高档位源端目标；单纯增加 CHR producer 线程会进一步放大 Flink 内存和 checkpoint 压力 | `benchmark-runner/output/benchmark-runs/starrocks-5rounds-6min-p6-t6-20260725-0549/index.html` |
| 2026-07-25 | `all-sinks-5rounds-6min-p6-t6-20260725-0212` / `all-sinks-5rounds-6min-p6-t6-20260725-0212-hive-cells40000-chr-eps30` | Hive，`40000` 小区，Target CHR `30/小区/s`，Global CHR `1200000 EPS`，Target PM `1/小区/s`，Source `360s`，Drain timeout `600s`，Flink 并行度 `6` | Hive 40000 档 source 发布约 `7624万` CHR，低于目标约 `4.32亿`；随后 Flink 作业进入 `RESTARTING/CANCELED`，`Source: chr-source` 标记 FAILED，后续 Iceberg 五档均在提交阶段失败，显示 slot 等待或容器重建相关错误 | 本机单 TaskManager 在 `1.2M CHR EPS` 目标下 CPU/调度压力过高，JobMaster 先记录 `TaskManager ... is no longer reachable`，重启申请 slot 时触发 `NoResourceAvailableException`；该故障发生在本地资源边界和跨轮恢复阶段，不代表 Hive/Iceberg sink 本身写入上限 | 本轮保留为本机资源边界样本；继续完整四类 Sink 评估前，应将高档位迁到 external-yarn/多 TaskManager 或降低本机最高档位，并在 benchmark 中增加跨 sink 阶段的 Flink runtime 健康恢复检查，避免一个高档位 TaskManager 心跳失败污染后续 Sink 提交 | `benchmark-runner/output/benchmark-runs/all-sinks-5rounds-6min-p6-t6-20260725-0212/index.html` |
| 2026-07-24 | `starrocks-2000-20260724-1950` / `starrocks-2000-20260724-1950-starrocks-cells2000-chr-eps30` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | Flink 提交阶段失败，StarRocks connector 校验报 `StarRocks does not have column source_event_ts_avg`，未进入 source 发数和 sink 写入 | 本轮代码新增 `source_event_ts_*` 延迟打点字段，但本地 StarRocks 物理表仍是旧 schema；benchmark 每轮只执行 `local prepare`，此前 StarRocks schema 收敛只在 `local init` 中执行，直接压测时不会补列 | 已将 `local prepare` 的 StarRocks 分支改为 reset 数据前先执行 connector schema 收敛；该失败不计入 StarRocks sink 能力判断，修复后用同档复跑 | `benchmark-runner/output/benchmark-runs/starrocks-2000-20260724-1950/index.html` |
| 2026-07-24 | `starrocks-operator-metrics-2000-20260724-1134` / `starrocks-operator-metrics-2000-20260724-1134-starrocks-cells2000-chr-eps30` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | `topology-service` 退出码 `1`，未进入有效压测；`benchmark-topology-service.err` 显示 `Topic topology not present in metadata after 15000 ms` | Kafka topic reset 后 broker metadata/producer 路径短时不可见，`benchmark-topology-service.out` 同期出现 `Bootstrap broker localhost:9092 disconnected`，属于共享 Kafka 初始化后的瞬时可见性问题 | 已重新执行 `bash scripts/deploy.sh local init` 重建 topics 并确认共享 Kafka、StarRocks、Hive 健康后复跑；该轮不计入 StarRocks sink 能力判断 | 无有效 report，目录：`benchmark-runner/output/benchmark-runs/starrocks-operator-metrics-2000-20260724-1134` |
| 2026-07-23 | `starrocks-2000-20260723-2219` / `starrocks-2000-20260723-2219-starrocks-cells2000-chr-eps30` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | Flink 作业正常运行并写入 StarRocks，但报告中的 `storage.records` 仍为 `0`；Java 级 probe 复现为 stdout 空或参数被错误解析 | Java 在 Windows 上通过 ProcessBuilder 调 Git Bash 执行 `bash -lc` 时，嵌套 `container_mysql="docker exec ..."` 和 Bash 数组参数会被 native 参数转义破坏，导致 probe 命令成功退出但没有有效输出 | 已将 StarRocks probe 改为无嵌套命令字符串、无 Bash 数组的直接参数式执行，并用 Java ProcessBuilder 实测返回 `starrocks rows=331218`；后续用新 jar 复跑验证报告 | `benchmark-runner/output/benchmark-runs/starrocks-2000-20260723-2219/index.html` |
| 2026-07-23 | `starrocks-2000-20260723-2201` / `starrocks-2000-20260723-2201-starrocks-cells2000-chr-eps30` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | Flink 作业正常运行并写入 StarRocks，但单轮报告 `storage-snapshot.json` 显示 `starrocks rows=0`；手动查库已有 `cell_kpi/user_anomaly/grid_anomaly/cell_anomaly` 可见数据 | StarRocks storage probe 在 host mysql 路径返回 `0` 行时直接采用结果，没有继续 fallback 到容器内 `starrocks-fe` 查询；本地环境下 host 路径可能连接到不可见或错误实例，导致报告采集失真 | 已将 `StarRocksStorageProbe` 改为 host 查询只有返回非 0 行才提前采用，否则继续执行容器内 mysql 查询；补充回归测试覆盖 host 0 行 fallback 场景 | `benchmark-runner/output/benchmark-runs/starrocks-2000-20260723-2201/index.html` |
| 2026-07-23 | `starrocks-2000-20260723-2132` / `starrocks-2000-20260723-2132-starrocks-cells2000-chr-eps30` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | `deploy submit` 未拿到 JobID，随后 Docker CLI 短时无响应；后台实际提交出的 Flink 作业进入 `RESTARTING`，TaskManager 没有可用 slot | Docker Desktop 的 `docker-desktop` WSL VM 发生全局内存压力：约 `15.8GB` 内存只剩约 `1GB`、`4GB` swap 几乎打满；内核 OOM kill 杀掉 `fdb-flink-taskmanager` 的 Java 进程，容器状态 `OOMKilled=true`、退出码 `137`，导致 Docker API/submit 链路卡顿和 Flink `NoResourceAvailableException` | 已取消残留 Flink 作业并重建 TaskManager；本机连续压测时将 TaskManager 从 `10240m` 降到 `6144m` 复跑，同步建议压测前检查 WSL available/swap，避免与无关高内存容器共同运行 | `benchmark-runner/output/benchmark-runs/starrocks-2000-20260723-2132/index.html` |
| 2026-07-23 | `starrocks-2000-20260723-2029` / `starrocks-2000-20260723-2029-starrocks-cells2000-chr-eps30` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | topology-service 发布 topology 时退出码 `1`；Flink 作业启动后 `cfg-source` 两次报 `Timeout of 60000ms expired before the position for partition cfg-config-* could be determined`，最终被 runner 取消 | benchmark reset topic 后立即启动 topology/Flink，Kafka 单 broker 仍在处理异步 topic 删除、controller 转发和 producer-id 分配；broker 日志同期出现 `Timed out waiting for next producer ID block`，topic list 可见但 producer metadata/idempotent producer 路径未完全恢复 | 已在 `local prepare` 的 Kafka reset 后增加 topic metadata 多轮稳定等待，并增加启用 idempotence 的极小 producer probe；后续只有 Kafka topic 与 producer-id 路径都恢复后才启动 topology 和 Flink，本轮不计入 StarRocks sink 能力判断 | `benchmark-runner/output/benchmark-runs/starrocks-2000-20260723-2029/index.html` |
| 2026-07-23 | `starrocks-2000-20260723-2017` / `starrocks-2000-20260723-2017-starrocks-cells2000-chr-eps30` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | topology-service 发布 topology 时退出码 `1`；Flink 同期 `cfg-source` 报 `Timeout of 60000ms expired before the position for partition cfg-config-* could be determined`，作业随后被取消 | Docker/WSL 恢复后 Kafka 控制面进入 unhealthy：内部 `localhost/kafka:9092` 健康检查超时，broker 日志反复出现 `Timed out waiting for next producer ID block` 和 broker-to-controller 连接超时；重启 Kafka 时又短暂遇到 ZooKeeper `/brokers/ids/1` 残留 | 等待 ZK broker 临时节点消失后重启 Kafka，确认 Kafka health 为 `healthy` 且 broker API 可用，再重新执行 `local init` 和 benchmark；该轮不计入 StarRocks sink 能力判断 | `benchmark-runner/output/benchmark-runs/starrocks-2000-20260723-2017/index.html` |
| 2026-07-23 | `starrocks-2000-20260723-2013` / `starrocks-2000-20260723-2013-starrocks-cells2000-chr-eps30` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | `deploy submit` 阶段失败，日志为 `Flink slots are not available for local submit`，未进入 source 发数和 sink 写入 | Docker Desktop 恢复后，Flink runtime 是按默认 `FDB_FLINK_TASKMANAGER_SLOTS=4` 启动的；benchmark env 要求 `FDB_FLINK_PARALLELISM=6`、`FDB_FLINK_TASKMANAGER_SLOTS=6`，可用 slot 少于作业并行度 | 用 benchmark env 重新执行 `local up` 重建 JobManager/TaskManager，确认 `/overview` 的 `slots-total/available` 均不小于 `6` 后复跑；该轮不计入 StarRocks sink 能力判断 | `benchmark-runner/output/benchmark-runs/starrocks-2000-20260723-2013/index.html` |
| 2026-07-23 | `starrocks-2000-20260723-1942` | StarRocks，`2000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s` | `deploy submit` 阶段未拿到 JobID；随后 Docker Desktop Linux engine 多次无响应，`docker ps/logs/info` 超时或返回 `500 Internal Server Error` | Docker Desktop 的 `docker-desktop` WSL VM 发生全局 OOM，内核日志显示无关容器 `everos-server` 占用约 `4.8GB` RSS 后被 OOM kill，Docker engine/API proxy 随后与 dockerd/containerd 失联；恢复过程中 Kafka 出现旧 broker session 残留，StarRocks FE/BE 因重建顺序出现 cluster id 不匹配 | 已终止并重启 `docker-desktop` WSL VM，恢复 Docker Desktop；重启 Kafka 后 topic 初始化恢复；清理并重建 StarRocks BE storage 卷，使 BE 重新注册到当前 FE 后复跑，本轮不计入 StarRocks sink 能力判断 | `benchmark-runner/output/benchmark-runs/starrocks-2000-20260723-1942/index.html` |
| 2026-07-23 | `iceberg-1000-load-20260723-1708` / `iceberg-1000-load-20260723-1708-iceberg-cells1000-chr-eps30` | Iceberg，`1000` 小区，Target CHR `30/小区/s`，Global CHR `30000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | `deploy prepare` 阶段 Kafka AdminClient `deleteTopics` 等待超时，未进入 Flink 提交和发数；runner 随后执行 `local stop` 时 Flink REST cancel `curl` 没有有界超时，在 REST 无响应时卡住；同期 Docker Desktop API 查询返回 `500 Internal Server Error` | Kafka broker/本地 Docker 控制面短时不可用导致 topic reset 无法完成；代码层面 `deleteTopics` 对单个 future `TimeoutException` 直接裸抛且可能消耗完整 reset deadline，`local stop` 的 cancel 请求缺少有界超时 | 已将 Kafka 单 topic delete future 超时按 retryable 处理并限制单次等待为轮询间隔；`local stop` REST cancel 默认增加 `--max-time 10`；当前需要恢复 Docker/Flink/Kafka 控制面后复跑，本轮不用于 Sink 能力判断 | `benchmark-runner/output/benchmark-runs/iceberg-1000-load-20260723-1708/index.html` |
| 2026-07-23 | `iceberg-fixedsim-5000-guarded-20260723-0034` / `iceberg-fixedsim-5000-guarded-20260723-0034-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | Flink 作业进入 `RESTARTING`，exception 为 `NoResourceAvailableException: Could not acquire the minimum required resources`；TaskManager 未 OOM，但 `/overview` 仅有 `4` slots，本轮手动终止 | 手动重建 Flink runtime 时绕过 `deploy.sh` 的 `.env.local` 加载，TaskManager 回落到 compose 默认 `FDB_FLINK_TASKMANAGER_SLOTS=4`，而作业 `FDB_FLINK_PARALLELISM=6`，source 发数开始后作业无法获得足够 slot | 已让 `local submit` 前置校验可用 slot 必须不少于 `FDB_FLINK_PARALLELISM`，并补充脚本回归测试；后续重建 Flink runtime 必须通过加载 `.env.local` 的命令执行 | 无有效 report，目录：`benchmark-runner/output/benchmark-runs/iceberg-fixedsim-5000-guarded-20260723-0034` |
| 2026-07-23 | `iceberg-fixedsim-5000-rerun-20260723-0021` / `iceberg-fixedsim-5000-rerun-20260723-0021-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | 本地 submit 阶段出现两个 `RESTARTING` 的 Flink 作业，`fdb-flink-taskmanager` 被 OOMKilled，退出码 `137`；未进入有效 source 发数阶段 | 首次 `flink run` 超时但实际已提交作业，脚本在 30 秒迟到 JobID 等待内未识别到，随后默认轻量重试又提交了第二个作业；双作业叠加导致本地 slot/内存不可用 | 已将 `local submit` 默认改为不重试未知提交结果，只延长等待迟到 JobID；需要重试时必须显式设置 `FDB_LOCAL_FLINK_SUBMIT_RETRY_ON_UNKNOWN=1`，并补充脚本回归测试 | 无有效 report，目录：`benchmark-runner/output/benchmark-runs/iceberg-fixedsim-5000-rerun-20260723-0021` |
| 2026-07-23 | `iceberg-fixedsim-5000-final-20260723-0010` / `iceberg-fixedsim-5000-final-20260723-0010-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | benchmark 在 `deploy prepare` 阶段退出，日志停在 `resetting shared Iceberg HDFS benchmark outputs`，未提交 Flink 作业 | 本轮人为将 `FDB_SHARED_HDFS_EXEC_TIMEOUT_SEC` 压到 `10s`，共享 HDFS 的 Iceberg 目录清理偶发超过该值，`hdfs dfs -mkdir/-rm` 被有界超时中断；用 `60s` 复现同路径可正常完成 | Iceberg/Hive 的 prepare 清理不再使用 `10s` 激进超时，压测命令恢复为默认/`60s` 级别；该失败不计入 Iceberg Sink 能力判断 | `benchmark-runner/output/benchmark-runs/iceberg-fixedsim-5000-final-20260723-0010/index.html` |
| 2026-07-23 | `iceberg-fixedsim-5000-cfgclose-20260722-2359` / `iceberg-fixedsim-5000-cfgclose-20260722-2359-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | topology-service 发布 topology 时 `localhost:9092` 一次 metadata 等待超时，日志为 `Topic topology not present in metadata after 60000 ms`；随后 Docker/Kafka topic 查询短时间慢响应，本轮手动取消 | benchmark 连续 reset topic 后，Kafka topic 已存在但 producer metadata 短时不可见；`KafkaTopologyPublisher` 直接进入逐条发送，metadata 不可见时会在 send 阶段阻塞/失败，且 close 没有有界超时 | 已让 topology publisher 在发送前先等待 topic metadata 可见，默认最长 `120s`，并将 producer `max.block.ms/request.timeout.ms/delivery.timeout.ms` 设置为较短可控值；`close()` 改为 5 秒有界超时，补充 `KafkaTopologyPublisherTest` 回归测试 | 无有效 report，目录：`benchmark-runner/output/benchmark-runs/iceberg-fixedsim-5000-cfgclose-20260722-2359` |
| 2026-07-22 | `iceberg-fixedsim-5000-20260722-2344` / `iceberg-fixedsim-5000-20260722-2344-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | CHR 指标已达到约 `299s`，PM 指标达到 `300s`，但 runner 一直等不到 simulator 退出；jstack 显示 CFG 停在 `CfgSimulator.run` 的长周期 `Thread.sleep`，CHR 停在 `KafkaProducer.close()` 等待未交付批次 | 新增“等待 simulator 自行退出”后暴露旧行为：CFG baseline 发布后默认常驻等待配置更新，之前依赖 runner 强停；CHR 在 Kafka 侧严重积压时，producer close 没有显式有界超时，收尾可能长时间等待 | 已让 CFG 在固定 `simulation.duration.sec` 模式下只发布 baseline 后退出；`KafkaPublisher.close()` 改为 5 秒有界 close，未交付记录保留在 source metrics；补充 `CfgSimulatorTest/KafkaPublisherTest` 回归测试 | 无有效 report，目录：`benchmark-runner/output/benchmark-runs/iceberg-fixedsim-5000-20260722-2344` |
| 2026-07-22 | `iceberg-fixed-sim-5000-selfstop-20260722-2330` / `iceberg-fixed-sim-5000-selfstop-20260722-2330-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | 源端最终指标显示 CHR 实际发数时长约 `273s`、PM 约 `197s`，不足目标 `300s`，本轮手动取消，不作为有效 5000 档结果 | runner 从启动 simulator 进程后开始 sleep 计时，随后强制 stop；但 CHR/PM 进程需要等待 topology、加载小区和初始化 producer，真正进入发数循环更晚，初始化耗时被计入了压测时长 | 已改为 simulator 在源端真实发数循环内按 `FDB_SIMULATION_DURATION_SEC` 自行退出，runner 等待 simulator 全部退出后再进入 drain；正数模拟时长不再用外部 sleep+stop 强停，并补充 `BenchmarkOrchestratorTest/JavaSimulatorProcessManagerTest` 回归测试 | 无有效 report，目录：`benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-selfstop-20260722-2330` |
| 2026-07-22 | `iceberg-fixed-sim-5000-probefix-20260722-2254` / `iceberg-fixed-sim-5000-probefix-20260722-2254-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | runner 运行约 `10min` 后仍未生成最终 summary；线程栈显示主线程阻塞在 `ProcessCommandRunner.waitFor`，调用方为 `IcebergStorageProbe`；Flink REST 同期多次超时，本轮手动终止 | Hive/Iceberg probe 内部虽加了 `FDB_HDFS_PROBE_TIMEOUT_SEC`，但 benchmark 观测路径仍走 `ProcessCommandRunner` 默认 `900s` 外层超时；当 `bash/docker exec/hdfs find` 或管道无法按预期退出时，会拖住 drain 循环 | 已为观测 probe 设置独立外层超时：默认 `20s`，可通过 `FDB_BENCHMARK_PROBE_COMMAND_TIMEOUT_SEC` 调整，未设置时优先复用 `FDB_HDFS_PROBE_TIMEOUT_SEC`；`ProcessCommandRunner` 超时时同步杀掉子进程树，并补充 `DefaultBenchmarkClientsTest/ProcessCommandRunnerTest/StorageProbeTest` 回归测试 | 本轮未生成最终 summary，目录：`benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-probefix-20260722-2254` |
| 2026-07-22 | `iceberg-fixed-sim-5000-final-20260722-2233` / `iceberg-fixed-sim-5000-final-20260722-2233-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | Flink 作业运行约 `740s` 后被手动取消，checkpoint `23/23` 成功且失败 `0`；runner 卡在 drain 阶段的 `IcebergStorageProbe`，执行 HDFS `-find /warehouse/iceberg -name '*.inprogress*'` 时无短超时，未生成最终 summary | storage probe 属于观测路径，但其 HDFS find 命令直接受 `ProcessCommandRunner` 默认 `900s` 总超时保护；在 Docker/HDFS 慢响应时会阻塞 drain 循环，导致“等待窗口处理完后结束压测”的控制逻辑失效 | 已为 Hive/Iceberg storage probe 增加 `FDB_HDFS_PROBE_TIMEOUT_SEC`，默认 `10s`，host hdfs 和 Docker fallback 两条路径都通过 `timeout` 包住，并补充 `StorageProbeTest` 回归测试 | 本轮未生成最终 summary，目录：`benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-final-20260722-2233` |
| 2026-07-22 | `iceberg-fixed-sim-5000-hdfstimeout-20260722-2232` / `iceberg-fixed-sim-5000-hdfstimeout-20260722-2232-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | benchmark 在 `deploy prepare` 阶段判定 `FAILED`：Kafka AdminClient `deleteTopics` 返回 `UnknownServerException`；随后发现 Kafka topic 只剩 `cell-kpi-1m` 等残缺状态，迟到 Flink 作业因 `chr-events/pm-stats/cfg-config` 不存在进入 `RESTARTING` | `KafkaTopicResetTool.deleteTopics` 对 broker 侧瞬时 `UnknownServerException` 直接失败，没有在有界时间内重试并用 topic 可见性校验实际删除结果，导致 topic reset 停在半初始化状态 | 已让 `deleteTopics` 对 `UnknownServerException` 做有界重试，每轮后通过 `listTopics` 判断 pending topic 是否实际删除，并补充 `KafkaTopicResetToolTest` 回归测试；脏 Flink 作业已通过 REST cancel 清理 | `benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-hdfstimeout-20260722-2232/index.html` |
| 2026-07-22 | `iceberg-fixed-sim-5000-lighretry-20260722-2217` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Simulation `300s`，Drain timeout `300s` | benchmark 卡在 `local prepare`，尚未提交 Flink 作业；进程树显示阻塞在 shared HDFS 的 `hdfs dfs -mkdir -p /warehouse/iceberg/iceberg_db/user_anomaly_events`，本轮手动终止 | `shared_hdfs_exec` 的 compose HDFS 路径没有有界超时，Docker/HDFS 某一次慢响应会拖住整个 benchmark prepare | 已为 `shared_hdfs_exec` 增加 `FDB_SHARED_HDFS_EXEC_TIMEOUT_SEC`，默认 `30s`；compose 路径超时后回退 direct `docker exec`，direct 路径也受同一超时保护，并补充脚本回归测试 | 无正式 report，日志：`logs/iceberg-fixed-sim-5000-lighretry-20260722-2217.out.log` |
| 2026-07-22 | `iceberg-fixed-sim-5000-cleanupfix-20260722-2206` / `iceberg-fixed-sim-5000-cleanupfix-20260722-2206-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，Simulation `300s`，Drain timeout `300s` | benchmark 在 `deploy submit` 阶段判定 `FAILED`，Flink 未产生新作业；首次 `flink run` 超时且没有 JobID 后，脚本触发 JobManager/TaskManager 重建，随后 Docker Desktop 查询 `flink:1.20.3-scala_2.12-java17` 镜像时返回 `500 Internal Server Error` | 提交失败恢复路径过重：Flink REST 已显示 slot 可用时，默认仍通过 `docker compose up --force-recreate jobmanager taskmanager` 重建 runtime；这会把一次提交命令超时放大成 Docker 镜像 API/容器重建风险 | 已将 `local_submit` 改为默认先轻量重试 `flink run` 并继续从 Flink REST 捕捉迟到 JobID；只有显式设置 `FDB_LOCAL_FLINK_SUBMIT_RECREATE_ON_RETRY=1` 时才重建 Flink runtime，并补充脚本回归测试 | `benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-cleanupfix-20260722-2206/index.html` |
| 2026-07-22 | `iceberg-fixed-sim-5000-observefix-20260722-2140` / `iceberg-fixed-sim-5000-observefix-20260722-2140-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，Simulation `300s`，Drain timeout `300s` | benchmark 判定 `UNSTABLE`：`1m window materialization lag: expected >= 2 closed minutes, CHR=1, PM=3, KPI=3`；CHR source 实际约 `3406 EPS`、积压约 `40508363` 条；收尾后发现 Flink 作业仍 RUNNING，手动取消后 checkpoint 失败累计 `2`，exception 包含 `TaskManager heartbeat timed out` 和 `NoResourceAvailableException` | 本地 5000*30 目标已经超过当前单机 producer/Kafka/Flink 组合的可持续能力，CHR 数据无法按 5 分钟目标量及时送达和消费；同时 `local_submit` 在 `flink run` 成功但 stdout 未打印 JobID 时没有 REST 兜底，导致 state 文件写入空 JobID，cleanup 无法自动取消作业 | 已让 `local_submit` 在 submit log 无 JobID 时从 Flink REST active job 兜底写入 JobID，并补充 `scripts/test-deploy-dispatch.sh` 回归测试；该轮不用于判断 Iceberg sink 上限，后续需复跑确认 cleanup 生效 | `benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-observefix-20260722-2140/index.html` |
| 2026-07-22 | `iceberg-fixed-sim-5000-scheduledflush-20260722-2123` / `iceberg-fixed-sim-5000-scheduledflush-20260722-2123-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，Simulation `300s`，Drain timeout `300s` | benchmark 判定 `FAILED: request timed out`；Flink 作业无 exception，最终由 runner 正常取消，checkpoint 完成 `12` 次且失败 `0`；source 原始文件显示 CHR 仅约 `6003 EPS`，积压约 `39570608` 条 | `FlinkRestClient` 对顶层 `/jobs/overview` 使用严格读取，Flink REST 在高压/收尾阶段一次超时会向外抛出并丢弃已采集的 source/topology 指标，导致报告落成无信息的 `FAILED` | 已将 `/jobs/overview` 改为降级读取，超时时生成 `UNKNOWN` Flink 快照；`UNKNOWN` 在决策中按观测不完整标记 `UNSTABLE`，保留 source/FDB/storage 证据，避免误报为链路执行失败；修复后重新打包并复跑 Iceberg 5000 档 | `benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-scheduledflush-20260722-2123/index.html` |
| 2026-07-22 | `iceberg-fixed-sim-5000-idleflush-20260722-2109` / `iceberg-fixed-sim-5000-idleflush-20260722-2109-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，Simulation `300s`，Drain timeout `300s` | benchmark 判定 `FAILED`，Flink 作业进入 `RESTARTING`，连续 checkpoint failure 为 `3`；runner 最终取消作业并释放 slot | `WindowMaterializationProbe` 在非 keyed stream 上调用 `ProcessFunction.Context.timerService().registerProcessingTimeTimer`，Flink 只允许 keyed stream 注册 timer；异常触发点为 `pm-1m-fact -> window-pm-1m-materialization` | 已将窗口物化探针改为内部低频调度线程，每 2 秒刷新有变化的当前窗口计数，关闭时再 flush；不额外 `keyBy`，避免为观测引入新的 shuffle；修复后重新打包并复跑 Iceberg 5000 档 | `benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-idleflush-20260722-2109/index.html` |
| 2026-07-22 | `iceberg-fixed-sim-5000-20260722-2031` / `iceberg-fixed-sim-5000-20260722-2031-iceberg-cells5000-chr-eps30` | Iceberg，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，Simulation `300s`，Drain timeout `300s` | Flink 作业无失败且算子侧 `chr-1m-fact`、`pm-1m-fact` 均输出 `25000` 条，但报告判定 `UNSTABLE`，原因为窗口物化指标只看到 CHR/KPI 各 `1` 个完整分钟 | `WindowMaterializationProbe` 按每条窗口输出异步发布 Kafka 指标，指标量过大；作业取消收尾时部分异步指标未被 Observability API 消费，导致窗口完成数被低估 | 已将窗口物化探针改为看到下一个窗口时发布上一个窗口的最终计数，并每 2 秒刷新当前窗口，关闭时再 flush；修复后重新打包并复跑 Iceberg 5000 档 | `benchmark-runner/output/benchmark-runs/iceberg-fixed-sim-5000-20260722-2031/index.html` |
| 2026-07-22 | `none-windowdiag-6g-heap010-http60-20260722-1626` / `none-windowdiag-6g-heap010-http60-20260722-1626-none-cells40000-chr-eps30` | None，`40000` 小区，Target CHR `30/小区/s`，Global CHR `1200000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，TaskManager process size `6144m`，managed fraction `0.10` | 前四档完成且 Flink failed task 为 `0`；第 5 档判定 `FAILED: request timed out`，事后 Docker 显示 TaskManager `OOMKilled=true`、退出码 `137`，Flink exception history 为空 | 即使关闭业务 sink，40000 档仍会让本地单 TaskManager 在 source/enrichment/1m 聚合/异常检测状态和 Kafka backlog 下触达容器级内存边界；这说明当前本机瓶颈在流计算和源侧积压，不是 StarRocks/Iceberg/Hive sink 独有 | 本机 None 报告保留，40000 档不作为有效上限；若要继续验证 1.2M CHR EPS，应迁移到 external-yarn/多 TaskManager，或先拆分 source producer 与 Flink runtime 资源后再复测 | `benchmark-runner/output/benchmark-runs/none-windowdiag-6g-heap010-http60-20260722-1626/index.html` |
| 2026-07-22 | `starrocks-windowdiag-6g-heap010-http60-20260722-1552` / `starrocks-windowdiag-6g-heap010-http60-20260722-1552-starrocks-cells40000-chr-eps30` | StarRocks，`40000` 小区，Target CHR `30/小区/s`，Global CHR `1200000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，TaskManager process size `6144m`，managed fraction `0.10`，benchmark HTTP timeout `60s` | 前四档完成且 Flink failed task 为 `0`；第 5 档仍判定 `FAILED: request timed out`，事后 Docker 显示 `fdb-flink-taskmanager` 为 `OOMKilled=true`、退出码 `137`，Flink exception history 为空 | HTTP 超时误判已缓解，但 40000 档 StarRocks 多业务 sink、异常 sink 与窗口状态在本地单 TaskManager 上触发容器级 OOM；这是本地资源边界，不是单纯观测超时 | 本机 StarRocks 五档报告保留，40000 档不作为有效上限；继续加大单 TM process size 会挤占 Docker/WSL 全局资源，优先建议 external-yarn/多 TaskManager 复测，或单独降低异常 sink/业务 sink 组合压力后再拆解瓶颈 | `benchmark-runner/output/benchmark-runs/starrocks-windowdiag-6g-heap010-http60-20260722-1552/index.html` |
| 2026-07-22 | `starrocks-windowdiag-6g-heap010-20260722-1511` / `starrocks-windowdiag-6g-heap010-20260722-1511-starrocks-cells40000-chr-eps30` | StarRocks，`40000` 小区，Target CHR `30/小区/s`，Global CHR `1200000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，TaskManager process size `6144m`，managed fraction `0.10` | 第 5 档报告判定 `FAILED`，原因为 `request timed out`；Flink 对应作业无 exception，最终 `CANCELED` 且 failed task 为 `0`，TaskManager 未 OOMKilled | 高压下 benchmark-runner 对 Flink/observability API 的单次 HTTP GET 固定 `10s` 超时过短，观测请求慢响应被误判为业务链路失败；该失败不是 StarRocks connector 写入异常 | 已将 `JdkHttpGateway` 的请求超时改为 `FDB_BENCHMARK_HTTP_TIMEOUT_SEC` 可配置，默认 `30s`，`.env.local` 设置为 `60s`；重新打包 benchmark-runner 后复跑 StarRocks 五档 | `benchmark-runner/output/benchmark-runs/starrocks-windowdiag-6g-heap010-20260722-1511/index.html` |
| 2026-07-22 | `iceberg-windowdiag-6g-20260722-1355` / `iceberg-windowdiag-6g-20260722-1355-iceberg-cells20000-chr-eps30` | Iceberg，`20000` 小区，Target CHR `30/小区/s`，Global CHR `600000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，TaskManager process size `6144m`，managed fraction `0.25` | 第 4 档报告判定 `FAILED`，原因为 `Flink job status RESTARTING`；Flink exception history 的首个业务异常是 `pm-source` Kafka fetcher 抛出 `java.lang.OutOfMemoryError: Java heap space`，随后重启阶段出现 `NoResourceAvailableException`；TaskManager 容器未 OOMKilled | 高档位下 CHR backlog 与 PM source fetch 同时占用 heap，本地单 TaskManager 的 task heap 不足；这不是 Iceberg writer/committer 的直接写入异常 | 本轮报告保留为本地 heap 边界样本；后续复跑在总 process size 仍为 `6144m` 的前提下下调 `FDB_FLINK_TASKMANAGER_MANAGED_FRACTION=0.10`，把更多内存留给 task heap；若仍失败，再考虑 external-yarn/多 TaskManager，而不是继续用单 TM 判断 Iceberg sink 上限 | `benchmark-runner/output/benchmark-runs/iceberg-windowdiag-6g-20260722-1355/index.html` |
| 2026-07-22 | `starrocks-windowdiag-6g-20260722-1303` / `starrocks-windowdiag-6g-20260722-1303-starrocks-cells20000-chr-eps30` | StarRocks，`20000` 小区，Target CHR `30/小区/s`，Global CHR `600000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，TaskManager process size `6144m` | 第 4 档报告判定 `FAILED`，原因为运行窗口末尾 Flink job 状态仍为 `RESTARTING`；事后 Flink 显示该作业已 `CANCELED`，但有 `1` 个 failed task，exception 为 `NoResourceAvailableException`，TaskManager 未再出现 `OOMKilled` | 本机单 TaskManager 在 600k CHR EPS 目标下已超过稳定资源边界，收尾取消/重启过程中触发 TaskExecutor shutting down 与 slot 重获竞争；该失败发生在高压收尾边界，不足以单独证明 StarRocks connector 写入上限 | 本轮保留报告作为本机资源边界样本，不用于判定 StarRocks sink 上限；后续同参数复测前先修复 benchmark-runner 对 simulator 子进程的退出等待与强制清理，避免跨轮残留污染，并优先在 external-yarn 或多 TaskManager 环境验证 20k 以上档位 | `benchmark-runner/output/benchmark-runs/starrocks-windowdiag-6g-20260722-1303/index.html` |
| 2026-07-22 | `starrocks-windowdiag-final2-20260722-1240` / `starrocks-windowdiag-final2-20260722-1240-starrocks-cells5000-chr-eps30` | StarRocks，`5000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6`，TaskManager process size `8192m` | 2000 档跑满并取消成功；5000 档启动约 77 秒后 Flink 进入 `RESTARTING/CANCELED`，`/taskmanagers` 为空，Docker 显示 `fdb-flink-taskmanager` 为 `OOMKilled=true`、退出码 `137`；随后 Docker Desktop API 连续返回 500，需要重启 Docker Desktop，Kafka 恢复时因 ZooKeeper 旧 broker ephemeral 节点短暂启动失败 | StarRocks exactly-once 多业务 sink、用户异常 sink 写入和窗口状态在 5000 档形成内存峰值；本机 shared infra 与 8G TaskManager 共享 Docker Desktop/WSL 资源池，系统 OOM killer 直接杀掉 TaskManager。Kafka 的 `NodeExists /brokers/ids/1` 是 Docker Desktop 重启后的 ZooKeeper 旧 session 残留 | 本轮报告不可用于判断 StarRocks sink 上限；已重启 Docker Desktop、重启 ZooKeeper 并恢复 Kafka。后续本机 StarRocks 复测使用 `FDB_FLINK_TASKMANAGER_MEMORY=6144m` 限制 TaskManager 峰值，避免系统 OOM；若仍失败，应迁移到 external-yarn 或多 TaskManager，而不是继续把本机单 TM 结果解释为 StarRocks 写入上限 | `benchmark-runner/output/benchmark-runs/starrocks-windowdiag-final2-20260722-1240` |
| 2026-07-22 | `iceberg-windowdiag-rerun-20260722-0000`、`starrocks-windowdiag-rerun-20260722-1143/1153/1208/1213`、`starrocks-windowdiag-final-20260722-1222` | Iceberg/StarRocks，五档 `2000/5000/10000/20000/40000` 小区，Target CHR `30/小区/s`，Target PM `1/小区/s`，Flink 并行度 `6` | Iceberg 40000 档在 `deploy prepare` 阶段因运行中修改 `deploy.sh` 触发 shell 语法错误；StarRocks 首次提交缺少 `FDB_KAFKA_BOOTSTRAP`，后续 StarRocks 探针、StarRocks reset 和 `docker exec flink run` 均出现过长时间阻塞；`1222` 重跑中第二档 Docker CLI 被中止后 Flink 仍迟到提交，形成孤儿作业 | 这是 benchmark 本地编排问题，不是 Sink 写入上限：`local prepare` 原先不按 Sink 收敛清理范围；`local submit` 先没有透传本地 Kafka/HDFS/StarRocks 运行变量，随后又透传全部 `FDB_*` 导致 Docker exec 参数过大；StarRocks 存储探针和 shared StarRocks exec 缺少有界超时；本地 Docker exec 超时后还可能出现迟到 JobID | 已按 `FDB_RESULT_SINK` 收敛 prepare 清理范围；本地 Flink 提交改为白名单透传运行必需变量；StarRocksStorageProbe 增加 `FDB_STARROCKS_PROBE_TIMEOUT_SEC` 有界查询；shared StarRocks exec 增加 `FDB_SHARED_STARROCKS_EXEC_TIMEOUT_SEC`；`local submit` 增加有界超时、迟到 JobID REST 捕捉和一次 runtime 重建重试；修复后重新跑 Iceberg/StarRocks/None 五档报告 | 修复前报告不用于判断 Sink 上限；修复后报告输出到 `benchmark-runner/output/benchmark-runs/` |
| 2026-07-21 | `hive-windowdiag-20260721-2019` / `hive-windowdiag-20260721-2019-hive-cells10000-chr-eps30` | Hive，`10000` 小区，Target CHR `30/小区/s`，Global CHR `300000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6` | 第 3 档运行中 PM simulator 每秒只发布 `1` 条，远低于预期 `10000/s`，当前五档压测手动中止，结果不可用于判断 Hive Sink 上限 | PM simulator 启动时 `TopologyClient.awaitReady` 只等待“读到非空 topology 且 1 秒无新数据”，高档位偶发只截取到首批 `1` 个 cell；CHR/CFG 同档已分别加载 `10000` 个 cell | 已让 CHR/PM/CFG simulator 按 benchmark 注入的 `FDB_TOPOLOGY_TARGET_CELLS` 等待目标小区数，并增加 `TopologyClientTest` 回归测试；修复后重新打包并重跑 Hive 五档 | `benchmark-runner/output/benchmark-runs/hive-windowdiag-20260721-2019` |
| 2026-07-21 | `iceberg-incremental-chr-20260721-0207` / `iceberg-incremental-chr-20260721-0207-iceberg-cells20000-chr-eps30` | Iceberg，`20000` 小区，Target CHR `30/小区/s`，Global CHR `600000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6` | 第 4 档提交后约 70 秒，Flink job 进入 `RESTARTING/CANCELED`；exception 为 `NoResourceAvailableException`，REST 显示 `taskmanagers=0/slots=0`；TaskManager 容器退出码 `239` | TaskManager 日志显示 `java.lang.OutOfMemoryError: Metaspace`；Iceberg/Hive/Hadoop 依赖和多个 Iceberg writer/committer 在连续五档本地压测中耗尽默认 Metaspace，随后 TaskExecutor 退出导致 slots 丢失 | 新增 `FDB_FLINK_TASKMANAGER_METASPACE`，local Docker TaskManager 默认支持配置，`.env.local` 设置为 `768m`，external-yarn submit 同步透传；重建 TaskManager 后重新跑 Iceberg 五档 | `benchmark-runner/output/benchmark-runs/iceberg-incremental-chr-20260721-0207` |
| 2026-07-20 | `none-30eps-scale-20260720-1448` / `none-30eps-scale-20260720-1448-none-cells10000-chr-eps30` | None，`10000` 小区，Target CHR `30/小区/s`，Global CHR `300000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6` | benchmark 判定 `FAILED`：Flink job 进入 `RESTARTING`；exception history 显示 `TaskManager heartbeat timed out`，随后 `NoResourceAvailableException`；CHR 实际约 `237648 EPS`，达成率约 `79.2%` | None Sink 下仍会执行 enrichment、1m/5m 聚合和异常检测；本机单 TaskManager 在约 `240k CHR EPS` 附近 CPU/资源余量不足，心跳被饿死后触发资源不可用 | 当前本地 None Sink 稳定上限更接近 `5000` 小区、约 `150k CHR EPS`；继续冲高应使用 external-yarn/多 TaskManager，或降低单 TaskManager 状态压力并扩展 source producer | `benchmark-runner/output/benchmark-runs/none-30eps-scale-20260720-1448/index.html` |
| 2026-07-20 | `none-30eps-scale-20260720-1358` / `none-30eps-scale-20260720-1358-none-cells40000-chr-eps30` | None，`40000` 小区，Target CHR `30/小区/s`，Global CHR `1200000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6` | benchmark 判定 `FAILED`：Flink job 进入 `RESTARTING`；Flink exception 为 `Java heap space`，失败算子是 `user-event-anomaly-detect-dedup`；CHR 实际约 `169322 EPS`，source backlog 约 `316484064`，undelivered 约 `982398` | None Sink 已去掉业务 sink 写入开销，但 40000 档输入目标远超当前本机单 TaskManager 和单机 producer 能力；TaskManager heap 在高压窗口状态/异常去重状态下耗尽，源端也只能达到目标的约 `14.1%` | 该档是本地资源上限信号，不应解释为下游 sink 上限；后续需要 external-yarn、多 TaskManager 或扩容 TaskManager heap，并把 CHR producer 拆成多进程/独立压测源后再评估高档位 | `benchmark-runner/output/benchmark-runs/none-30eps-scale-20260720-1358/index.html` |
| 2026-07-20 | `none-30eps-scale-20260720-1358` / `none-30eps-scale-20260720-1358-none-cells5000-chr-eps30` | None，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6` | 业务观测期 CHR 约 `150k EPS` 且 Flink 无 failed task，但报告被标记为 `FAILED`：`deploy stop failed`，原因是 cancel 后等待 job 终态超时 | 高压取消阶段 Flink job 从 `CANCELLING` 到 `CANCELED` 超过旧默认等待时间；这是压测收尾脚本阈值过短导致的误判，不是 None Sink 处理失败 | 已移除 `local stop` 后等待 slot 的误判，并把 `FDB_FLINK_CANCEL_WAIT_SEC` 默认值提升到 `300s`；`local submit` 增加 TaskManager 缺失时的自动重建，避免上一档取消后 slot 丢失影响下一档提交 | `benchmark-runner/output/benchmark-runs/none-30eps-scale-20260720-1358/index.html` |
| 2026-07-19 | `starrocks-30eps-scale-20260719-1148` / `starrocks-30eps-scale-20260719-1148-starrocks-cells20000-chr-eps30` | StarRocks，`20000` 小区，Target CHR `30/小区/s`，Global CHR `600000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6` | 五档压测均跑满 5 分钟并生成总报告；20000 档取消后 JobManager 显示 `taskmanagers=0/slots=0`，需要手动重启 TaskManager 后继续 40000 档；最终 10000/20000/40000 均因源端吞吐达成率不足判定 `UNSTABLE` | 本机单 TaskManager + 单机 simulator/Kafka 入口在 150k-220k CHR EPS 附近达到可持续边界，高档位源端 backlog 快速增长；20000 档取消时 TaskManager CPU 打满导致 heartbeat timeout，但 metrics Kafka producer 已受限为有限超时关闭，没有再出现无限 close | 当前报告不能解释为 StarRocks sink 极限；应先把 source/Kafka 生成侧扩为多进程或迁到 external-yarn，再评估 StarRocks 上限。对本机报告可把 5000 小区、约 `150k EPS` 作为接近源端达标的最高档位 | `benchmark-runner/output/benchmark-runs/starrocks-30eps-scale-20260719-1148/index.html` |
| 2026-07-19 | `starrocks-30eps-scale-20260719-1142` / 全部档位 | StarRocks，`2000/5000/10000/20000/40000` 小区，Target CHR `30/小区/s` | benchmark 在 `deploy prepare` 阶段全部失败，Flink 未提交；HDFS 清理报 `mkdir: No FileSystem for scheme "C"` | Windows Git Bash/Docker 参数转换把 HDFS 目录参数转成了 `C:` 盘符路径；临时最小 env 文件也没有完整复用本地基础设施配置，放大了路径转换问题 | 已在 `scripts/deploy.sh` 固化 `MSYS2_ARG_CONV_EXCL=*`，并改用 full env 覆盖文件重新执行 1148 批次 | `benchmark-runner/output/benchmark-runs/starrocks-30eps-scale-20260719-1142/index.html` |
| 2026-07-19 | `starrocks-30eps-scale-20260719-1039` / `starrocks-30eps-scale-20260719-1039-starrocks-cells5000-chr-eps30` | StarRocks，`5000` 小区，Target CHR `30/小区/s`，Global CHR `150000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6` | 5000 档跑满观测窗口后取消，TaskManager 日志出现 `OutOfMemoryError`，REST 后续显示 `taskmanagers=0/slots=0`；runner 已启动 10000 档 simulator 但 submit 等不到 slot | runner 在上一档 Flink 资源未释放前先启动下一档 simulator，且清理阶段先取消 Flink 后停 simulator，失败后会继续向 Kafka 放大积压；同时本机 TaskManager heap 在 150k CHR EPS + StarRocks sink 压力下余量不足 | 已调整 runner 顺序为先提交 Flink 再启动 simulator、清理时先停 simulator 再取消 Flink，并让 `local stop` 等 job 终态和 slot 恢复；重建后重新跑本轮压测 | 无正式最终报告，故障定位后手动中止 |
| 2026-07-18 | `starrocks-scale-20260718-132035-cells40000` / `starrocks-scale-20260718-132035-cells40000-starrocks-cells40000-chr-eps10` | StarRocks，`40000` 小区，Target CHR `10/小区/s`，Global CHR `400000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，CHR producer 线程 `6` | benchmark 判定 `FAILED`：`request timed out`；原始 CHR source metrics 显示实际约 `84299 EPS`，发布约 `7040688` 条，backlog 约 `25367853`，PM 实际约 `36317 EPS`；失败快照中 TaskManager 为 `0` | 本机 4w * 10 CHR 目标远超当前单机 producer/Flink runtime 可承载范围，观测请求在重压下超时；压测后 Flink JobManager 仍可访问，但 TaskManager 掉线，Docker API 查询也出现超时/500 | 该档不能用于判断 StarRocks sink 上限，应先恢复 Docker/Flink runtime；后续 400k CHR EPS 需要 external-yarn 或独立压测机/多 producer，再重新评估 sink 极限 | `benchmark-runner/output/benchmark-runs/starrocks-scale-20260718-132035-cells40000/index.html` |
| 2026-07-18 | `starrocks-scale-20260718-132035-cells20000` / `starrocks-scale-20260718-132035-cells20000-starrocks-cells20000-chr-eps10` | StarRocks，`20000` 小区，Target CHR `10/小区/s`，Global CHR `200000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，CHR producer 线程 `6` | benchmark 判定 `UNSTABLE`：`source throughput attainment 0.9456878717224209`；CHR 实际约 `189138 EPS`，PM 实际约 `19503 EPS`，CHR backlog 约 `357458`，undelivered 约 `742015`；checkpoint failure 为 `1` | 源端吞吐没有达到目标，当前本机 CHR producer/Kafka 发送链路在约 `190k EPS` 附近开始欠产，尚未把压力稳定传递到 StarRocks sink | 先提高 CHR producer 线程或改为多进程/独立压测源，再重跑 20k 档；在源端达标前不要把本轮结果解释为 StarRocks 写入瓶颈 | `benchmark-runner/output/benchmark-runs/starrocks-scale-20260718-132035-cells20000/index.html` |
| 2026-07-18 | `starrocks-scale-20260718-132035-cells10000` / `starrocks-scale-20260718-132035-cells10000-starrocks-cells10000-chr-eps10` | StarRocks，`10000` 小区，Target CHR `10/小区/s`，Global CHR `100000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，CHR producer 线程 `6` | benchmark 判定 `UNSTABLE`：`watermark lag 182563 ms`；CHR 实际约 `99952 EPS`、PM 实际约 `10037 EPS`，源端基本达标；checkpoint duration 约 `5361 ms`，checkpoint failure 为 `0`，Flink backpressure 为 `0` | 源端和 checkpoint 没有明显失败，但 1 分钟窗口链路的事件时间滞后超过 `180s` 稳定性阈值，说明本机单 TaskManager 在 100k CHR EPS 下已经不能满足当前实时性门限 | 可把 100k CHR EPS 作为本机当前不稳定边界；若要继续判断 StarRocks sink 上限，需要扩 Flink runtime 或迁到 external-yarn，避免被本机实时性门限提前截断 | `benchmark-runner/output/benchmark-runs/starrocks-scale-20260718-132035-cells10000/index.html` |
| 2026-07-18 | `starrocks-10k-threaded-20260718-114130` / `starrocks-10k-threaded-20260718-114130-starrocks-cells10000-chr-eps10` | StarRocks，`10000` 小区，Target CHR `10/小区/s`，Global CHR `100000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6`，CHR producer 线程 `6` | benchmark 判定 `UNSTABLE`：`watermark lag 187582 ms`；CHR 实际约 `99968 EPS`、PM 实际约 `10042 EPS`，源端已基本达标；Flink 无 backpressure、checkpoint failure 为 0，但本轮停止后 `cell_kpi/kpi_1m/kpi_5m=0`，`user_anomaly_events=341489`、`grid_anomaly_events=1645` | 1m KPI 首次输出晚于 checkpoint 4 完成时间，进入 StarRocks exactly-once 后续事务；runner 因 watermark lag 超过 `180s` 阈值提前取消作业，checkpoint 5 被 abort，KPI sink 已 prepare 的事务没有进入 commit/visible 阶段，因此最终 KPI 表为空 | 多线程 CHR producer 已解决 10k 小区下源端产能不足；后续应把 StarRocks exactly-once 压测的停止策略改为等待一次包含业务 sink 输出的成功 checkpoint，或在压测策略中将 `watermark lag` 超阈值与“需要收尾 checkpoint 提交”的报告阶段解耦，再重跑 10k 基线 | `benchmark-runner/output/benchmark-runs/starrocks-10k-threaded-20260718-114130/index.html` |
| 2026-07-18 | `starrocks-high-50k-hotspot-20260718-1013` / `starrocks-high-50k-hotspot-20260718-1013-starrocks-cells50000-chr-eps10` | StarRocks，`50000` 小区，Target CHR `10/小区/s`，Global CHR `500000 EPS`，Target PM `1/小区/s`，Flink 并行度 `6` | benchmark 判定 `UNSTABLE`：`source throughput attainment 0.17417310236480898`；CHR 实际约 `87086 EPS`，source backlog 约 `19974500`；StarRocks 写入无失败，`grid_anomaly_events` 已不再为 0，表内实际 `COVERAGE_HOLE=28`，但由于提前停止 `cell_kpi=0` | 本轮压力目标从历史全局 `15000 EPS` 提升为 50k 小区 * 每小区 `10 EPS` = 全局 `500000 EPS`，超过当前本机单 CHR simulator/单 Kafka producer 的生成与发送能力；runner 在第一次观测即按源端达标率判定 unstable，尚未进入完整 1 分钟 KPI 出数和多轮 checkpoint 提交阶段 | StarRocks connector 本身本轮没有暴露写入失败；后续若要压 50k * 10 的 CHR 目标，需要先把 CHR source 做成多进程/多 producer 或迁移到 external-yarn/独立压测机；本机继续验证 StarRocks sink 时应先用低于约 `85k EPS` 的 CHR 档位建立稳定基线，再逐步升压 | `benchmark-runner/output/benchmark-runs/starrocks-high-50k-hotspot-20260718-1013/index.html` |
| 2026-07-17 | `starrocks-high-50k-rerunfix2-20260717-225933` / `starrocks-high-50k-rerunfix2-20260717-225933-starrocks-cells50000-eps15000` | StarRocks，`50000` 小区，目标 CHR `15000 EPS`，Flink 并行度 `6` | writer 文件锁修复后 CHR 持续发布到约 `2072280` 条、`14987 EPS`，但 benchmark-runner 判定 `FAILED`：`source metrics path is not a file`；事后该路径恢复为合法 JSON 文件 | runner 读取 source metrics 的瞬间撞上 simulator 替换 metrics 文件的短暂窗口，`SourceMetricsSnapshot.readRaw` 对缺失或非普通文件没有重试，直接把瞬时状态当成致命错误 | 已为 `SourceMetricsSnapshot.readRaw` 增加有界重试，并新增 `retries_when_metrics_path_is_temporarily_not_a_file` 回归测试；重打包后重新运行同档 StarRocks 压测 | `benchmark-runner/output/benchmark-runs/starrocks-high-50k-rerunfix2-20260717-225933/index.html` |
| 2026-07-17 | `starrocks-high-50k-rerunfix-20260717-224548` / `starrocks-high-50k-rerunfix-20260717-224548-starrocks-cells50000-eps15000` | StarRocks，`50000` 小区，目标 CHR `15000 EPS`，Flink 并行度 `6` | CHR NPE 修复后源恢复到约 `14968 EPS`，但 CHR simulator 在约 58 秒后退出；stderr 显示 `failed to write source metrics ... 另一个程序正在使用此文件`，报告最终仍判定 `UNSTABLE`，原因是 `KPI availability p95 over threshold` | benchmark-runner 读取 source metrics JSON 的同时，simulator 用 `Files.move` 替换同一个 metrics 文件；Windows 对被其他进程占用的目标文件拒绝替换，`SourceMetricsWriter` 将该异常向上抛出导致 CHR 子进程退出 | 已为 `SourceMetricsWriter` 的临时文件替换增加有界重试，并新增 Windows 独立进程锁文件回归测试；重打包后重新运行同档 StarRocks 压测 | `benchmark-runner/output/benchmark-runs/starrocks-high-50k-rerunfix-20260717-224548/index.html` |
| 2026-07-17 | `starrocks-high-50k-rerun-20260717-223329` / `starrocks-high-50k-rerun-20260717-223329-starrocks-cells50000-eps15000` | StarRocks，`50000` 小区，目标 CHR `15000 EPS`，Flink 并行度 `6` | benchmark 进程退出码为 0，但报告判定 `UNSTABLE`，原因是 `source throughput attainment 0.0`；`chr-source-metrics` 显示 `published=0/durationMs=0`，PM 和 CFG 源正常发布，Flink 算子吞吐全为 0 | `ChrSimulator.generateEvent` 对非异常、非 `DATA_SESSION` 事件生成可空字段时，三元表达式把 `null` 自动拆箱为 `long`，触发 `NullPointerException` 后 CHR simulator 子进程提前退出 | 已将 CHR 可空字段先显式装箱为 `Long/Float` 再传给 Avro builder，并新增 `normal_events_allow_nullable_optional_measurement_fields` 回归测试；重打包后重新运行同档 StarRocks 压测 | `benchmark-runner/output/benchmark-runs/starrocks-high-50k-rerun-20260717-223329/index.html` |
| 2026-07-17 | `starrocks-high-50k-p6-20260717-1437` / `starrocks-high-50k-p6-20260717-1437-starrocks-cells50000-eps15000` | StarRocks，`50000` 小区，目标 CHR `15000 EPS`，Flink 并行度 `6` | Flink/StarRocks 仍无失败，checkpoint failure 为 0、TaskManager 未 OOM、watermark lag 约 `130182 ms`；benchmark 判定 `UNSTABLE`，原因仍是 `KPI availability p95 over threshold`，`starrocks-kpi-1m p95` 约 `250888 ms` | 降一档压力后尾部 sink 延迟仍接近 `250s`，说明当前瓶颈不是单纯输入规模；历史 metrics 显示上游 `kpi-1m p95` 约 `130503 ms`，StarRocks sink 前置探针在尾部批次跳到 `250s`，本机单 TaskManager 下 sink 路径存在固定尾部排队 | 暂不通过降低业务规模继续掩盖问题；后续应优先改进观测口径，区分 KPI 出数延迟、sink 排队延迟和真实写入完成延迟，并用 external-yarn 或多 TaskManager 验证 StarRocks exactly-once 的资源上限 | `benchmark-runner/output/benchmark-runs/starrocks-high-50k-p6-20260717-1437/index.html` |
| 2026-07-17 | `starrocks-high-60k-p6-20260717-1426` / `starrocks-high-60k-p6-20260717-1426-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS`，Flink 并行度 `6` | watermark lag 从上一轮 `184020 ms` 降到 `130238 ms`，checkpoint、TaskManager 和 StarRocks 均无失败；benchmark 仍判定 `UNSTABLE`，原因是 `KPI availability p95 over threshold`，`starrocks-kpi-1m p95` 约 `251322 ms` | 提高并行度解决了 watermark 滞后，但暴露了 StarRocks sink 路径尾部延迟；上游 `kpi-1m p95` 约 `130741 ms`，sink 前置探针约 `253925 ms`，StarRocks connector 日志显示 success load 7 次、failed load 0、write block 0，主要是 exactly-once sink 路径在本机高压下形成尾部排队 | 保持 exactly-once 口径，下一轮降到 `50000` 小区、目标 CHR `15000 EPS` 建立本机稳定基线；若要继续冲高，需要外部 YARN/更多 TaskManager 或单独评估 StarRocks at-least-once/flush 策略的性能与可靠性取舍 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-p6-20260717-1426/index.html` |
| 2026-07-17 | `starrocks-high-60k-resetretry-20260717-1412` / `starrocks-high-60k-resetretry-20260717-1412-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | Flink 跑完整个测量窗口，无 failed task、OOM、checkpoint failure 或 storage error；benchmark 判定 `UNSTABLE`，原因是 `watermark lag 184020 ms` | 本地单 TaskManager/4 并行度在 `18k EPS` 下接近实时性阈值，主要滞后来自 `pm-source 182344 ms` 和 `enrichment 184020 ms`；StarRocks sink `p95=133930 ms` 且失败数为 0 | 提高本地 Flink 并行度/slot 与 TaskManager 内存后重启 runtime 复测；若仍超过 `180s`，再下调小区规模建立稳定基线，或迁移到 external-yarn 验证集群资源上限 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-resetretry-20260717-1412/index.html` |
| 2026-07-17 | `starrocks-high-60k-recovered2-20260717-1404` / `starrocks-high-60k-recovered2-20260717-1404-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 在 `deploy prepare` 阶段失败，Flink 未提交；错误为 `UnknownTopicOrPartitionException: cell-anomaly-events does not exist` | Kafka topic 删除是异步的，删除后短时间内 create 返回 `TopicExistsException/marked for deletion`；旧 `KafkaTopicResetTool` 将该异常当作成功，随后配置 topic 时实际 topic 仍不存在 | `KafkaTopicResetTool` 改为删除后等待消失、创建后等待可见；遇到 marked-for-deletion 时重试创建，并新增回归测试；同时为 benchmark-runner 增加 `WARN` 日志配置，避免 Kafka DEBUG 日志污染报告 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-recovered2-20260717-1404/index.html` |
| 2026-07-17 | `starrocks-high-60k-tcpprobe-20260717-1325` / `starrocks-high-60k-tcpprobe-20260717-1325-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 在 `deploy prepare` 阶段失败，日志为多次 `service "kafka" is not running`；Flink 未提交 | Kafka topic 删除在 `docker exec ... kafka-topics --delete` 的 10 秒 direct timeout 内完不成，脚本进入 docker compose fallback；当前 shared infra 由直接容器运行，compose fallback 对 `kafka` service 识别不稳定 | 新增 `KafkaTopicResetTool`，`local prepare` 优先通过宿主机 `FDB_KAFKA_HOST_BOOTSTRAP` 使用 Kafka AdminClient 删除/创建 benchmark topics，jar 不存在或显式 `FDB_LOCAL_KAFKA_RESET_IMPL=docker` 时才回退旧 Docker 路径 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-tcpprobe-20260717-1325/index.html` |
| 2026-07-17 | `starrocks-high-60k-buffer64m-20260717-1309` / `starrocks-high-60k-buffer64m-20260717-1309-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | Flink 作业运行约 401 秒后被 runner 正常取消，作业无 failed task、TaskManager 未 OOM；benchmark 判定 `UNSTABLE`，原因是 storage probe 返回 Docker Desktop API `500 Internal Server Error` | StarRocks 健康探测在宿主机没有 `mysql` 客户端时直接走 `docker exec shared-data-infra-starrocks-fe-1 ... SELECT 1`，Docker Desktop 容器 API 间歇性异常会把健康 StarRocks 误判为不健康 | `StarRocksStorageProbe` 改为优先使用宿主机 `127.0.0.1:9030` TCP 端口探测，端口不可达时才退回 `docker exec`；重打包 benchmark-runner 后复测 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-buffer64m-20260717-1309/index.html` |
| 2026-07-17 | `starrocks-high-60k-buffer16m-20260717-1302` / `starrocks-high-60k-buffer16m-20260717-1302-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 在 `deploy submit` 阶段失败，Flink 未生成作业；异常为 `Unsupported value '30000' for 'sink.buffer-flush.max-rows'. Supported value range: [64000, 5000000]` | 新增的本地 StarRocks connector 缓冲降档配置过低；当前 connector 版本要求 `max-bytes >= 64MB`、`max-rows >= 64000` | 将 `.env.local` 改为合法下限 `67108864/64000/5000ms`，并在 `StarRocksSinks` 增加本地范围校验，避免再次提交非法参数 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-buffer16m-20260717-1302/index.html` |
| 2026-07-17 | `starrocks-high-60k-managed025-fix3-20260717-1244` / `starrocks-high-60k-managed025-fix3-20260717-1244-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 运行约 63 秒后判定 `FAILED`，Flink 先报 `TaskManager ... is no longer reachable`，随后 `NoResourceAvailableException`；Docker 显示 `fdb-flink-taskmanager` 为 `OOMKilled=true`、退出码 `137` | 单 TaskManager/4 并行度下同时存在 5 个 StarRocks 业务 sink，connector 默认 `sink.buffer-flush.max-bytes=90MB` 会按 subtask 叠加缓冲，叠加窗口状态、Kafka fetch 和本地 shared infra 后触发容器内存上限 | 已将 StarRocks connector 的 `sink.buffer-flush.max-bytes/max-rows/interval-ms` 做成环境变量，并在 `.env.local` 下调到 connector 合法下限 `64MB/64000/5000ms` 后重建 Flink runtime 复测；若 60k 仍不稳定，再降到 40k/12k 建立本机稳定基线 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-managed025-fix3-20260717-1244/index.html` |
| 2026-07-17 | `starrocks-high-60k-managed025-fix2-20260717-1238` / `starrocks-high-60k-managed025-fix2-20260717-1238-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 判定 `FAILED`，原因 `Flink job status RESTARTING`；报告显示 `taskManagers=0`、`slots=0`，Flink exception 为 `NoResourceAvailableException` | `fdb-flink-taskmanager` 在提交前已经 `Exited (137)`，导致作业没有可用 slot；这是上一轮 OOM/容器退出后的残留环境问题，不是本轮 StarRocks 写入阶段瓶颈 | 使用 `.env.local` 中 `FDB_FLINK_TASKMANAGER_MANAGED_FRACTION=0.25` 重建 TaskManager，确认 `/taskmanagers` 有可用 slot 后再复测；后续 benchmark 前应先检查 Flink runtime 健康 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-managed025-fix2-20260717-1238/index.html` |
| 2026-07-17 | `starrocks-high-60k-managed025-fix-20260717-1220` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 卡在 `deploy.sh local prepare`，尚未提交 Flink 作业，无正式 report | shared Kafka topic 清理阶段的 `docker exec shared-data-infra-kafka-1 kafka-topics --delete` 间歇挂住；同时 benchmark runner 对外部命令没有总超时，导致可无限等待 | `shared_kafka` / `shared_kafka_exec` 对 direct 和 compose Kafka exec 增加 `FDB_SHARED_KAFKA_EXEC_TIMEOUT_SEC` 超时；`ProcessCommandRunner` 增加 `FDB_BENCHMARK_COMMAND_TIMEOUT_SEC` 总超时；复测前已验证 `local prepare` 可完成 | 无 |
| 2026-07-17 | `starrocks-high-60k-managed025-20260717-1207` / `starrocks-high-60k-managed025-20260717-1207-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 判定 `FAILED: request timed out`；Flink 运行期间无异常，但报告未抓到 Flink/operator/topology 快照；收尾阶段 `docker exec flink cancel` 挂住 | observability API 的严格读取在压力下触发 JDK HTTP `request timed out`，导致整轮失败；`local stop` 先走 Docker CLI，Docker CLI 挂住会拖住 runner 收尾 | observability 指标采集改为超时降级为空指标，保留 Flink/Storage 判断；`local stop` 改为优先 Flink REST cancel，Docker CLI 仅作 fallback；重打包后复测 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-managed025-20260717-1207/index.html` |
| 2026-07-17 | `starrocks-high-60k-ask60-20260717-1117` / `starrocks-high-60k-ask60-20260717-1117-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 判定 `FAILED`，TaskManager 数量为 0；Docker 显示 `OOMKilled=true`、退出码 `137` | 将 `FDB_FLINK_TASKMANAGER_MANAGED_FRACTION` 调到 `0.15` 后 task heap 过大，叠加本机 shared infra 和高压数据生成，容器总内存被打满；后续 Flink `NoResourceAvailable` 是连带结果 | 将 managed fraction 回调到 `0.25`，保留 `pekko.ask.timeout=60s` 和 Kafka fetch 限流，重启 TaskManager 后从 60k 档继续复测 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-ask60-20260717-1117/index.html` |
| 2026-07-17 | `starrocks-high-20260717-0955` / `starrocks-high-20260717-0955-starrocks-cells80000-eps24000` | StarRocks，`80000` 小区，目标 CHR `24000 EPS` | Flink job 进入 `RESTARTING`，benchmark 判定 `FAILED` | `pm-source` Kafka fetch 触发 `java.lang.OutOfMemoryError: Java heap space`；StarRocks 健康检查正常，尚未压到 StarRocks sink 上限 | 调大 Flink TaskManager heap/process memory；如仍失败，再降低 Kafka source `fetch.max.bytes`、`max.partition.fetch.bytes` 或 `max.poll.records` 后重跑 | `benchmark-runner/output/benchmark-runs/starrocks-high-20260717-0955/index.html` |
| 2026-07-17 | `starrocks-high-mem8g-20260717-1015` | StarRocks，`80000` 小区，目标 CHR `24000 EPS` | 前置 topology-service 运行超过 5 分钟仍未提交 Flink 作业，无正式 report | `TopologyGenerator.allocatePci` 每次扫描全部历史 PCI，48 万 cell 下退化为平方级 CPU 开销 | 已改为只检查最近 2 个 PCI 的固定窗口，并重建 `topology-service`；继续同档复测 | 无 |
| 2026-07-17 | `starrocks-high-fixedtopo-20260717-1022` / `starrocks-high-fixedtopo-20260717-1022-starrocks-cells80000-eps24000` | StarRocks，`80000` 小区，目标 CHR `24000 EPS` | Flink job 进入 `RESTARTING`，TaskManager 数量变为 0，benchmark 判定 `FAILED` | Docker 显示 `fdb-flink-taskmanager` 为 `OOMKilled=true`、退出码 `137`；8G TaskManager 在当前本机 shared infra 下内存峰值过高，后续 `NoResourceAvailable` 是连带结果 | 已取消失败作业；TaskManager 调整为 `6144m`，并新增 Kafka source `fetch.max.bytes`、`max.partition.fetch.bytes`、`max.poll.records` 限流配置后继续复测 | `benchmark-runner/output/benchmark-runs/starrocks-high-fixedtopo-20260717-1022/index.html` |
| 2026-07-17 | `starrocks-high-mem6g-fetchcap-20260717-1033` / `starrocks-high-mem6g-fetchcap-20260717-1033-starrocks-cells80000-eps24000` | StarRocks，`80000` 小区，目标 CHR `24000 EPS` | benchmark 判定 `UNSTABLE`，原因是 `watermark lag 700188 ms` | 历史 Kafka topic 未清理，`cfg-config` 约 410 万条、`pm-stats` 超过 2.6 亿条；`cfg-source` 从 `earliest` 读取 compact topic，重放历史配置导致水位线落后 | 本地重置压测相关 Kafka topics 后重新初始化 topics，再继续同档复测 | `benchmark-runner/output/benchmark-runs/starrocks-high-mem6g-fetchcap-20260717-1033/index.html` |
| 2026-07-17 | `starrocks-high-clean-20260717-1042` / `starrocks-high-clean-20260717-1042-starrocks-cells80000-eps24000` | StarRocks，`80000` 小区，目标 CHR `24000 EPS` | 清理 topic 后仍进入 `RESTARTING`，benchmark 判定 `FAILED` | Flink exception history 首个根因是 `Heartbeat of TaskManager ... timed out`；TaskManager 未 OOM，但 CPU 长时间打满，心跳被饿死后触发 `NoResourceAvailable` | 增加 Flink heartbeat timeout/interval 配置，避免本地高压 CPU 抖动误判 TaskManager 失联；继续同档复测 | `benchmark-runner/output/benchmark-runs/starrocks-high-clean-20260717-1042/index.html` |
| 2026-07-17 | `starrocks-high-heartbeat-20260717-1055` / `starrocks-high-heartbeat-20260717-1055-starrocks-cells80000-eps24000` | StarRocks，`80000` 小区，目标 CHR `24000 EPS` | benchmark 判定 `UNSTABLE`，原因是 `watermark lag 188463 ms` | heartbeat 修复后作业未重启，但 24k EPS 下本地单 TaskManager/4 并行度已超过 180s watermark 目标 | 降一档压力到 `60000` 小区、目标 CHR `18000 EPS` 复测；若仍不稳定，再考虑增加并行度/TaskManager 或上 external-yarn | `benchmark-runner/output/benchmark-runs/starrocks-high-heartbeat-20260717-1055/index.html` |
| 2026-07-17 | `starrocks-high-60k-heartbeat-20260717-1107` / `starrocks-high-60k-heartbeat-20260717-1107-starrocks-cells60000-eps18000` | StarRocks，`60000` 小区，目标 CHR `18000 EPS` | benchmark 判定 `UNSTABLE`，原因是 `checkpoint failures 2`；取消阶段出现 `Java heap space` | JobManager 触发 checkpoint 到 TaskManager 的 RPC 使用默认 `pekko.ask.timeout=10s`，本地高 CPU 下超时；同时 `6144m` TaskManager 的 task heap 约 2.3GB，StarRocks connector 和窗口状态在取消/flush 阶段 heap 余量不足 | 新增 `FDB_FLINK_PEKKO_ASK_TIMEOUT=60s`，并设置 `FDB_FLINK_TASKMANAGER_MANAGED_FRACTION=0.15`，在不提高容器总内存的前提下增加 task heap 后复测 | `benchmark-runner/output/benchmark-runs/starrocks-high-60k-heartbeat-20260717-1107/index.html` |
